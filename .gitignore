# Data artifacts — large, rebuilt by pipeline/, uploaded to R2 (never committed)
data/
*.pmtiles
*.mbtiles
*.gpkg
*.fgb
# …except the small committed test fixtures (CC BY clips ~3.5 MB/5 MB/1 MB + four synthetic 2–3 KB rasters)
!app/tests/fixtures/*.pmtiles

# From Forest to Fjaeldmark content — NOT redistributable (see docs/LICENSING.md)
# (only the derived f2f_index.json of page numbers may be committed)
f2f_pdfs/
*.pdf

# Pipeline caches
pipeline/cache/
pipeline/work/
__pycache__/

node_modules/
dist/
.DS_Store
*.log
test-results/
playwright-report/

# wrangler local state (account metadata)
.wrangler/
